module test;
reg clk, reset, enable ;
wire [3:0]count;

counter_debug dut ( .clk(clk), .reset(reset), . enable(enable), .count(count));

always
#5 clk = ~ clk;

initial begin
clk =0;
reset = 0;
enable = 0;

#10; reset =1; enable = 1;
#50; enable = 0;
#10; enable = 1;

#100 $finish;
end

initial begin
$monitor ( " time = %t, clk = %b, reset = %b, count = %b ",$time,clk,reset,count);
end
endmodule
